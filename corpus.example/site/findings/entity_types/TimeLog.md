---
tags: [entity-type, schema, custom-field, inspector]
scope: site
title: TimeLog
verdict: On this site TimeLog has 0 sg_ fields and 0 vocabularies over 1012 rows. The codes here are what the API stores; the labels are editable.
---
# TimeLog

What this site configures on top of the shipped `TimeLog` card. The card above is the API layer; everything here is one site's own.

1012 rows site-wide, across every project. The 100 most recent were read for the link census below.

**Links populated site-wide**

| field | data type | set on | points at |
|---|---|---|---|
| `entity` | entity | 100/100 | Task 100 |

1 of 2 link fields are empty on every sampled row. Which ones a single project fills is the project layer.
